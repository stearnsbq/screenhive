apiVersion: apps/v1
kind: Deployment
metadata:
  name: ScreenHive-Websocket-Backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ScreenHive-Websocket-Backend
  template:
    metadata:
      labels:
        app: ScreenHive-Websocket-Backend
    spec:
      containers:
      - name: ScreenHive-Websocket-Backend
        image: <Image>
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
        ports:
        - containerPort: 3000
---

apiVersion: v1
kind: Service
metadata:
  name: ScreenHive-Websocket-Backend
spec:
  type: NodePort
  selector:
    app: ScreenHive-Websocket-Backend
  ports:
  - port: 80
    targetPort: 3000

